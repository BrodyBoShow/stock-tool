# Base URL of the Phase 9 FastAPI server (no trailing slash).
# Dev: run `uvicorn api.main:app --reload` from the repo root.
VITE_API_URL=http://localhost:8000

# Supabase auth — SAME project as the landing page (ref oqweuhwaiknnqupzkmxt).
# VITE_SUPABASE_URL  : https://<project-ref>.supabase.co
# VITE_SUPABASE_ANON_KEY : the anon / publishable key (safe to ship; it's the
#                          same key embedded in landing/index.html).
VITE_SUPABASE_URL=https://oqweuhwaiknnqupzkmxt.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
